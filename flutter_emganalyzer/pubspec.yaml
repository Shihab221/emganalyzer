name: emganalyzer
description: EMG Analyzer — Flutter companion for the same Vercel/Next backend.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  intl: ^0.20.2
  fl_chart: ^0.69.0
  google_fonts: ^6.2.1
  path_provider: ^2.1.5
  share_plus: ^10.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
