# Dev / one-time: export RandomForest .pkl -> fatigue_rf.json (used by Next.js at runtime).
#   pip install -r model/requirements.txt
#   python model/export_rf_json.py

numpy>=1.23
scikit-learn>=1.6,<1.8
